// Header: site header showing only the brand label for now — nav links
// and other content are added once real page content exists.
// Takes no props. Returns the <header> markup.
function Header() {
  return (
    <header>
      <span>Timsies Entirety</span>
    </header>
  )
}

export default Header
